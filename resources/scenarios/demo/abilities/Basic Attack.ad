BasicAttack = 
{
	name = "Basic Attack",
	description = "Attacks the target Card, dealing damage equal to your Attack Points.",
	
	effect = (target) ->
		{
			if (this.parent.parent == target.parent)
				return;
			
			target.setHealth(target.health - this.parent.attack, this);
		}
}
Intimidate = 
{
	name = "Intimidate",
	description = "Prevent any enemy Support Cards from being played next Turn.",
	
	effect = (target) ->
		{
			self = this.parent.parent;
			self.parent.getOther(self).setPlayableCardLimit(Card.SUPPORT, 0, this);
		}
}